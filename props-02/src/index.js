/* se non viene specificato il name prop, semplicemente non verrà renderizzato, quindi
rimarrà solo "welcome", è comunque possibile settare dei parametri di default 
in questo modo: */

Welcome.defaultProps = {
    name: "Marco"
}